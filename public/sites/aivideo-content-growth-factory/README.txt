星河 AIVideo 内容增长工厂 - 受控销售页源码

本版本完成：
1. 首屏节奏重排：Hero 高度、主视觉尺寸、文字层级、CTA、统计区统一增强。
2. 卡片区间距优化：问题卡、系统流、能力卡、行业卡、产品界面卡增加留白和统一阴影。
3. 导航/按钮/统计区统一高级感：固定磨砂导航、统一圆角和渐变按钮、玻璃统计面板。
4. 16 张生成图片均已使用，并按网站位置重新整理到 assets 目录。

在 XingheWeb 仓库中运行 `npm run dev` 后，可通过 `/sites/aivideo-content-growth-factory/index.html` 预览页面。
本目录已自包含页面所需的 HTML、CSS、JS、logo、二维码与图片素材，不依赖 AIVideo 原仓库的 `素材` 目录，也不依赖 `public/images`。
对外发布必须先通过 AIVideo 对应销售话术与 production 上线证据包校验。
